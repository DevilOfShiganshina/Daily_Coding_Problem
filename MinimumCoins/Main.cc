#include <iostream>

short min_num_of_coins(short total) {
  short retval = 0;
  short coins[] = {25, 10, 5, 1};

  while (total != 0) {
    
  }
}

int main() {
  short num;
  std::cout << "Enter an integer: ";
  std::cin >> num;

  std::cout << min_num_of_coins(num) << std::endl;

  return 0;
}